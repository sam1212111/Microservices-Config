server:
  port: 7077

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/restaurantapp
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
